apiVersion: v1
kind: Service
metadata:
  name: devops-platform
  namespace: devops-platform
spec:
  selector:
    app.kubernetes.io/name: devops-platform
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
